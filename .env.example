# Comma-separated list of allowed origins for CORS (required in production)
# Example: https://your-website.vercel.app,https://your-dashboard.vercel.app
# Use * to allow all origins (development only)
# CORS_ORIGINS=
